space & tab:: {
  send("{" codeObj["hankaku"] "}")
}
space & q:: {
  send("#{4}")
}
space & w:: {
  send("#{3}")
}
space & e:: {
  send("#{2}")
}
space & r:: {
  send("#{1}")
}
space & t:: {
  send("{0}")
}

space & y:: {
  send("+{Home}")
}
space & u:: {
}
space & i:: {
  send("+{Left}")
}
space & o:: {
  send("+{Right}")
}
space & p:: {
  send("^+{Right}")
}
space & sc1A:: {
  send("+{End}")
}
