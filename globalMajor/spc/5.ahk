space & LCtrl:: {
  fxPenMode(
    (*) => (send("^+{d}")),
    (*) => send("{" codeObj["enter"] "}")
  )
}
space & LWin:: {
  send("{F12}")
}
space & sc38:: {
  send("{F11}")
}
space & sc7B:: {
  send("{F2}")
}

space & sc79:: {
}
